/** @format */

export const drawPlayerText = (ctx, playerInfo) => {
  console.log("player info and text is drawn here");

  ctx.clearRect(0, 0, ctx.canvas.width, ctx.canvas.height);
  ctx.font = "14px, Arial";
  ctx.fillStyle = "whitesmoke";

  for (let index of playerInfo) {
  }
};
